**[12] 함수 (8문항)**

- 함수를 왜 사용하나요?

- 함수 선언문으로 선언된 함수는 엔진에서 어떻게 접근하고 호출하나요?

- 함수 선언문으로 정의한 함수와 함수 표현식으로 정의한 함수의 생성과 할당 시점은 어떻게 되나요?

- 객체 타입 인수를 지양해야하는 이유는 무엇인가요?

- 고차함수란 무엇인가요?

- 매개변수의 기본값은 어떻게 설정하나요?

- 함수 외부에서 함수 이름으로 함수를 호출할 수 있나요?

- 아래 코드를 실행한 결과는 무엇인가요?

  ```js
  const factorial = function foo(n) {
    return n * factorial(n - 1);
  };

  console.log(factorial(5));
  ```
